<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <meta name="author" content="">
    <title>InstagramRails</title>
    <link rel="icon" href="/favicon.ico">

    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', 'album', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <div class="collapse bg-dark" id="navbarHeader">
        <div class="container">
          <div class="row">
            <div class="col-sm-8 col-md-7 py-4">
              <h4 class="text-white">About</h4>
              <p class="text-muted">Rails application, which allows the user to view their Instagram pictures.</p>
            </div>
            <div class="col-sm-4 offset-md-1 py-4">
              <% if current_user %>
              <h4 class="text-white">Hi, <%= current_user.username %></h4>
              <ul class="list-unstyled">
                <li>
                  <%= link_to('My Pictures', media_items_path, class:"text-white") %>
                </li>
                <li>
                  <%= link_to('Sign Out', destroy_user_session_path, method: :delete, class:"text-white") %>
                </li>
              </ul>
              <% else %>
              <h4 class="text-white">Hi, </h4>
              <ul class="list-unstyled">
                <li>
                  <%= link_to "Login", omniauth_authorize_path(:user, :instagram), class: "text-white" %>
                </li>
              </ul>
              <% end %>

            </div>
          </div>
        </div>
      </div>
      <div class="navbar navbar-dark bg-dark box-shadow">
        <div class="container d-flex justify-content-between">
          <a href="/" class="navbar-brand d-flex align-items-center">
            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="mr-2"><path d="M23 19a2 2 0 0 1-2 2H3a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h4l2-3h6l2 3h4a2 2 0 0 1 2 2z"></path><circle cx="12" cy="13" r="4"></circle></svg>
            <strong>InstagramRails</strong>
          </a>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarHeader" aria-controls="navbarHeader" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
        </div>
      </div>
    </header>

    <%= render partial: "/layouts/flash_messages" %>

    <%= yield %>

    <footer class="text-muted">
      <div class="container">
        <p class="float-right">
          <a href="#">Back to top</a>
        </p>
        <p>InstagramRails &copy;</p>
      </div>
    </footer>
  </body>
</html>
